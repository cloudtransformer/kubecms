apiVersion: v1
kind: Service
metadata:
  namespace: kubecms
  name: kubecms-data
  labels:
    app: kubecms
    service: kubecms-data
spec:
  selector:
    app: kubecms
    service: kubecms-data
  ports:
    - protocol: TCP
      port: 5001
      targetPort: 80